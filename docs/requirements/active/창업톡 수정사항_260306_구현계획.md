# Bug Fix 实施计划

**Source:** `창업톡 수정사항_260306.pptx` (13 pages)
**Created:** 2026-03-06
**Status:** ✅ 实施完成

---

## 수정사항 요약 (修改事项总结)

总计 **8 个** 修改事项（Page 1-8），Page 9-13 为 TOPS 设计参考。

---

## Phase A: Quick Fixes（API / i18n / 配置）

### Issue 1: 约款编辑保存不生效 (Page 1)

**现象:** 管理员编辑利用条款后保存无反应
**优先级:** 🔴 高
**影响范围:**
- `frontend/src/shared/services/content.service.js`

**修改方向:**
- 添加 `updateLegalContent(type, data)` 方法，调用 `PUT /api/admin/content/legal/{type}`
- 后端接口已存在（`content/router.py` line 833），仅前端缺失

---

### Issue 3 + 6: 用户页面通知显示英文 (Page 3, 6)

**现象:** 管理员页面显示正常，但用户页面通知显示英文 key 路径
**优先级:** 🔴 高
**影响范围:**
- `frontend/src/member/modules/support/locales/ko.json`
- `frontend/src/member/modules/support/locales/zh.json`

**修改方向:**
- `notifications.member.approved` 从字符串改为 `{ subject, content }` 嵌套结构
- `notifications.member.rejected` 同上
- 保留原始字符串作为向后兼容（不保留，直接改结构，因为 `notificationParser.js` 生成的 key 路径必须匹配）

---

## Phase B: Editor 增强

### Issue 4 + 8: 表格固定 3×3 / 链接添加问题 (Page 4, 8)

**现象:** 插入表格只能 3×3；系统介绍页无法添加链接
**优先级:** 🟡 中
**影响范围:**
- `frontend/src/shared/components/TiptapEditor.jsx`

**修改方向:**
- 表格：将硬编码 `{ rows: 3, cols: 3 }` 改为弹窗输入行/列数
- 链接：Link extension 已配置，需确认是否为 UI 可见性问题（复用已有 linkModal）

---

## Phase C: 注册字段补全

### Issue 5 + 6: 注册时字段缺失 + 信息未传递 (Page 5, 6)

**现象:** 注册时部分字段（电话、代表电话等）未保存到数据库
**优先级:** 🔴 高
**影响范围:**
- `backend/src/modules/user/schemas.py` — MemberRegisterRequest
- `backend/src/modules/user/service.py` — register_member()
- `frontend/src/member/modules/auth/services/auth.service.js` — 字段映射

**修改方向:**
- Schema 添加: `phone`, `representative_phone`, `contact_person_phone`, `contact_person_department`, `contact_person_position`, `startup_type`, `startup_stage`, `ksic_major`, `ksic_sub`, `category`, `business_field`
- Service 添加: 将新增字段存入 member_data
- Frontend 添加: auth.service.js 映射缺失的字段

---

## Phase D: 待确认项

### Issue 2: 横幅图片重复 (Page 2)

**现象:** 横幅图片重复显示
**优先级:** 🟡 中
**状态:** 需运行时确认具体横幅数据

### Issue 7: 分页不生效 (Page 7)

**现象:** 所有页面显示全部 15 条数据
**优先级:** 🔴 高
**状态:** 代码逻辑正确，需运行时排查具体页面

---

## 实施顺序

1. Phase A: Quick Fixes → Issue 1, 3, 6
2. Phase B: Editor → Issue 4, 8
3. Phase C: Registration → Issue 5, 6
4. Phase D: Runtime verification → Issue 2, 7
