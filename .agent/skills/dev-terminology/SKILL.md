---
name: dev-terminology
description: 项目术语字典专家。Use when (1) naming new entities, (2) creating schemas, (3) writing tests, (4) defining API endpoints. Ensures naming consistency across frontend, backend, and database.
---

# 项目术语字典工作流

确保项目中所有命名遵循统一约定，保持前后端、测试、数据库的一致性。

## 触发场景

- 创建新模型/Schema
- 定义 API 端点
- 编写测试用例
- 添加新的业务实体
- 创建前端组件/状态

## 核心原则

### 1. 一词一义

每个术语只有一个明确定义，避免使用同义词造成混淆。

### 2. 层级一致

同一概念在不同层级使用相同术语：

- 数据库表名 → ORM 模型 → API Schema → 前端类型 → 测试

### 3. 语言约定

| 层级         | 语言    | 示例                      |
| ------------ | ------- | ------------------------- |
| 代码标识符   | English | `user_id`, `ChatMessage`  |
| 注释/文档    | 中文    | `// 用户认证服务`         |
| API 错误消息 | 中文    | `"邮箱已被注册"`          |
| 日志消息     | English | `"User login successful"` |

---

## 术语字典

> 📖 **完整术语表**: 见 `references/glossary.md`

---

## 命名约定

### 后端 (Python)

| 类型      | 约定              | 示例                         |
| --------- | ----------------- | ---------------------------- |
| 类名      | PascalCase        | `UserService`, `ChatMessage` |
| 函数/方法 | snake_case        | `get_by_id`, `create_user`   |
| 变量      | snake_case        | `user_id`, `access_token`    |
| 枚举      | PascalCase        | `UserRole.ADMIN`             |
| 文件名    | snake_case        | `user_service.py`            |
| 数据库表  | snake_case (复数) | `users`, `documents`         |
| 数据库列  | snake_case        | `user_id`, `created_at`      |

### 前端 (TypeScript)

| 类型      | 约定       | 示例                        |
| --------- | ---------- | --------------------------- |
| 类型/接口 | PascalCase | `User`, `ChatMessage`       |
| 函数      | camelCase  | `getUserById`, `createUser` |
| 变量      | camelCase  | `userId`, `accessToken`     |
| 枚举      | PascalCase | `UserRole.ADMIN`            |
| 文件名    | PascalCase | `ChatMessage.tsx`           |
| CSS 类    | kebab-case | `user-card`, `chat-message` |

### API 端点

| 类型     | 约定       | 示例                               |
| -------- | ---------- | ---------------------------------- |
| 路径     | kebab-case | `/api/v1/users`                    |
| 查询参数 | snake_case | `?page_size=10&sort_by=created_at` |
| 请求体   | snake_case | `{"user_id": "..."}`               |

---

## 使用指南

### 添加新术语

1. 确定术语的业务含义
2. 选择简洁明确的英文名
3. 定义各层级的命名
4. 更新 `references/glossary.md`
5. 同步更新枚举文件

### 检查命名一致性

创建新代码时，检查：

- [ ] 术语是否在字典中
- [ ] 命名是否遵循层级约定
- [ ] 枚举值是否一致
- [ ] 测试中是否使用相同命名

---

## 相关文件

| 文件                                 | 描述         |
| ------------------------------------ | ------------ |
| `references/glossary.md`             | 完整术语字典 |
| `backend/app/core/enums.py`          | 后端枚举定义 |
| `frontend/src/shared/types/enums.ts` | 前端枚举定义 |

---

**记住**：一致的命名是可维护代码的基础。当不确定时，先查字典！
