{
  "name": "hooks",
  "description": "React Hooks 文件规范",
  "exampleFile": "hooks-example.js",

  "sections": [
    { "name": "useAuth", "alias": "认证", "prefix": "useAuth" },
    { "name": "useDebounce", "alias": "防抖", "prefix": "useDebounce" },
    { "name": "useLocalStorage", "alias": "本地存储", "prefix": "useLocalStorage" },
    { "name": "usePagination", "alias": "分页", "prefix": "usePagination" },
    { "name": "useToggle", "alias": "开关", "prefix": "useToggle" },
    { "name": "useUpload", "alias": "文件上传", "prefix": "useUpload" }
  ],

  "naming": {
    "hookPrefix": "use",
    "caseStyle": "camelCase"
  },

  "required": {
    "reactImports": ["useState", "useEffect", "useCallback"]
  },

  "forbidden": {
    "patterns": ["console.log", "debugger"],
    "directDomAccess": true
  },

  "codeStyle": {
    "functionKeyword": true,
    "useCallback": true,
    "singleLineComment": true,
    "requireSectionComment": true,
    "indent": 2
  },

  "rules": [
    "Hook 命名必须以 use 开头",
    "使用 function 关键字定义 hook",
    "回调函数使用 useCallback 包装",
    "使用单行中文注释",
    "每个 hook 前需要分隔注释",
    "禁止直接操作 DOM"
  ]
}
